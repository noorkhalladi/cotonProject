<form method="post" action="{{ path('bordereau_reg_delete', {'id': bordereau_reg.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bordereau_reg.id) }}">
    <button class="btn">Delete</button>
</form>
