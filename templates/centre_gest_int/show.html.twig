{% extends 'base.html.twig' %}

{% block title %}CGI{% endblock %}

{% block body %}
    <h1><center>Centre de Gestion des Intrant</center></h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ centre_gest_int.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ centre_gest_int.code }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ centre_gest_int.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('centre_gest_int_index') }}">back to list</a>

    <a href="{{ path('centre_gest_int_edit', {'id': centre_gest_int.id}) }}">edit</a>

    {{ include('centre_gest_int/_delete_form.html.twig') }}
{% endblock %}
