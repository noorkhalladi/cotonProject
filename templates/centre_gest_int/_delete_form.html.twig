<form method="post" action="{{ path('centre_gest_int_delete', {'id': centre_gest_int.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ centre_gest_int.id) }}">
    <button class="btn">Delete</button>
</form>
