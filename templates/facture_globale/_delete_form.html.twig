<form method="post" action="{{ path('facture_globale_delete', {'id': facture_globale.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facture_globale.id) }}">
    <button class="btn">Delete</button>
</form>
